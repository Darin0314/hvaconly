server {
    listen 8093;
    server_name localhost;
    root /home/smokeshow/code/hvaconly/newsite/extracted/hvac_08042026/hvac;
    index index.html;

    location / {
        try_files $uri $uri/ $uri.html =404;
    }

    location ~ \.php$ {
        include snippets/fastcgi-php.conf;
        fastcgi_pass unix:/var/run/php/php8.3-fpm.sock;
    }
}
